<%=titulo_pagina(link_icone("Usuarios","users"),"Listagem de usuarios")%>
<table class='table'>
  <thead>
    <tr>
      <th>Nome</th>
      <th>E-mail</th>
      <th>Permissão</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @usuarios.each do |usuario| %>
      <tr>
        <td><%= usuario.pessoa.nome %></td>
        <td><%= usuario.email %></td>
        <td><%= usuario.role %></td>
        <td><%= link_to link_icone('detalhes'), usuario,:class=>"btn btn-success btn-sm" %></td>
        <td><%= link_to link_icone('editar'), edit_usuario_path(usuario),class: "btn btn-warning btn-sm",data: { no_turbolink: true }  %></td>
        <td><%= link_to link_icone('apagar'), usuario, method: :delete, data: { confirm: 'Are you sure?' },:class=>"btn btn-danger btn-sm" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>



<%= link_to link_icone('Cadastrar Usuario','plus'), new_usuario_path, :class=>"btn btn-large btn-success",data: { no_turbolink: true }  %>
