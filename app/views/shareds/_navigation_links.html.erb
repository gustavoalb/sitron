<%# add navigation links to this file %>
<% if user_signed_in? %>
    <li class="dropdown">
                <a href="#" class="dropdown-toggle username" data-toggle="dropdown"><span class="hidden-xs"><%=current_user.nome%> <i class="fa fa-caret-down"></i></span><%=image_tag("padrao/avatar_default.png")%></a>
                <ul class="dropdown-menu userinfo arrow">
                    <li class="username">
                        <a href="#">
                            <div class="pull-left"><%=image_tag("padrao/avatar_default.png",:class=>"userimg")%></div>
                            <div class="pull-right"><h5>Olá, <%=current_user.pessoa.nome%>!</h5></div>
                        </a>
                    </li>
                    <li class="userlinks">
                        <ul class="dropdown-menu">
                            <li><a href="<%=edit_user_registration_path%>">Editar Conta<i class="pull-right fa fa-pencil"></i></a>
                            </li>
                            <li class="divider"></li>
                            <li>
                             <%= link_to 'Sair', destroy_user_session_path, :method=>'delete',:class=>"text-right" %>
                        </ul>
                    </li>
                </ul>
            </li>


<% else %>
<li>
	<%= link_to 'Entrar', new_user_session_path %>
</li>
<% end %>
<% if user_signed_in? %>
<% if current_user.administrador? %>
<li>
	<%= link_to 'Users', users_path %>
</li>
<% end %>
<% end %>

